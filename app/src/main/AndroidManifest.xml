<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplicationtry">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SortActivity"></activity>
        <activity android:name=".BillActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".FormsSubmittedActivity" />
        <activity android:name=".RequestDisplayActivity" />
        <activity android:name=".ShowSelectedSchoolsActivity" />
        <activity android:name=".ClassesDisplayActivity" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".Form" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SchoolDisplay" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>